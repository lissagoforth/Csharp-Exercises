using System;

namespace calculator
{
    partial class Program
    {
        static void Main(string[] args)
        {
            // Calculator newCalc = new Calculator();

            // int AddedNumber = newCalc.Add(2,2);
            // Console.WriteLine(AddedNumber);

            // int SubtractedNumber = newCalc.Subtract(98, 52);
            // Console.WriteLine(SubtractedNumber);

            // int MultiplyedNumber = newCalc.Multiply(8, 7);
            // Console.WriteLine(MultiplyedNumber);

            // int DividededNumber = newCalc.Divide(84, 3);
            // Console.WriteLine(DividededNumber);


        }
    }
}
